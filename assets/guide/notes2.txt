------------------------------------------------------------------ Check Package 
find / -iname ufw
find / -iname .kube
find / -type f -exec grep -l "kube" {} +
grep -Rnw '/' -e 'kube'

------------------------------------------------------------------ Add sbin to path
// add to path because cannot find ufw
export PATH=$PATH:/usr/sbin

// or add that at the end of the bashrc file
nano ~/.bashrc
export PATH=$PATH:/usr/sbin

------------------------------------------------------------------ check logs
// check logs
journalctl -u k3s.service --since "10 minutes ago"
journalctl -xeu k3s.service | tail -n 50

------------------------------------------------------------------ check ports listening
ss -tuln