apiVersion: v1
kind: Namespace
metadata:
  name: mnamespace
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mdeployment
  namespace: mnamespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mdeployment
  template:
    metadata:
      labels:
        app: mdeployment
    spec:
      containers:
      - name: whoami-c
        image: traefik/whoami
        ports:
        - containerPort: 80
---

apiVersion: v1
kind: Service
metadata:
  name: mservice
  namespace: mnamespace
spec:
  type: ClusterIP  # Change NodePort to ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: mdeployment
